# Test: xorq (bitwise XOR), result is zero
# Covers: XOR operation, ZF flag set when result is 0
#
# Expected result:
#   %rax = 0   (0xFF ^ 0xFF = 0)
#   ZF=1, SF=0, OF=0

    irmovq $255, %rax
    irmovq $255, %rbx
    xorq   %rbx, %rax
    halt
